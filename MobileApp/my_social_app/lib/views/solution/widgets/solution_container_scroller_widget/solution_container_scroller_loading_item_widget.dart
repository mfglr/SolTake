import 'package:flutter/material.dart';

class SolutionContainerScrollerLoadingItemWidget extends StatelessWidget {
  const SolutionContainerScrollerLoadingItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
      ],
    );
  }
}